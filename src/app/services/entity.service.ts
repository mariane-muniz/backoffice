import { Injectable } from '@angular/core';
import { Entity } from '../entities/entity';

@Injectable({
  providedIn: 'root'
})
export class EntityService {

  constructor() { }

  getLastRegisteredEntities(): void {
  }
}
